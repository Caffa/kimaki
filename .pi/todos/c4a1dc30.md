{
  "id": "c4a1dc30",
  "title": "Add local voice transcription features to upstream Kimaki",
  "tags": [
    "feature",
    "local-voice",
    "vision",
    "kimaki"
  ],
  "status": "open",
  "created_at": "2026-06-07T06:02:04.280Z"
}

## Features to Port

### 1. Local Voice Banner (cli/src/cli-runner.ts)
- Add figlet import
- Add `showLocalTranscriptionBanner()` function with ASCII art
- Call banner on startup

### 2. Vision Routing (cli/src/vision-description.ts)
- Create new file for vision description using local Ollama models
- Add fallback chain for vision: cloud → local vision → local fallback
- Implement `describeImage()` function

### 3. Voice Transcription Changes (cli/src/voice.ts)
- Keep Parakeet MLX (local)
- Keep vLLM Whisper (local)
- Remove OpenAI fallback
- Remove Gemini fallback

### 4. Dependencies (cli/package.json)
- Add `figlet` and `@types/figlet`

## Implementation Order
1. Add dependencies first
2. Add vision-description.ts
3. Modify voice.ts for local-only transcription
4. Add banner to cli-runner.ts
5. Build and test
