#!/usr/bin/env tsx
"use strict";
// Generates the idempotent SQLite bootstrap schema consumed at Kimaki startup.
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
Object.defineProperty(exports, "__esModule", { value: true });
var node_fs_1 = require("node:fs");
var node_path_1 = require("node:path");
var exec_async_js_1 = require("../src/exec-async.js");
var root = process.cwd();
var outputPath = node_path_1.default.join(root, 'src/schema.sql');
function makeSchemaIdempotent(sql) {
    return sql
        .split('\n')
        .map(function (line) {
        return line
            .replace(/^CREATE TABLE\b(?! IF NOT EXISTS)/i, 'CREATE TABLE IF NOT EXISTS')
            .replace(/^CREATE UNIQUE INDEX\b(?! IF NOT EXISTS)/i, 'CREATE UNIQUE INDEX IF NOT EXISTS')
            .replace(/^CREATE INDEX\b(?! IF NOT EXISTS)/i, 'CREATE INDEX IF NOT EXISTS');
    })
        .filter(function (line) { return !/^CREATE TABLE IF NOT EXISTS [`"]?sqlite_sequence[`"]?/i.test(line.trim()); })
        .join('\n');
}
function main() {
    return __awaiter(this, void 0, void 0, function () {
        var result, rawSql, firstSqlLine, sql;
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    console.log('Exporting Drizzle schema SQL...');
                    return [4 /*yield*/, (0, exec_async_js_1.execAsync)('pnpm drizzle-kit export', { cwd: root })];
                case 1:
                    result = _a.sent();
                    rawSql = result.stdout.trim();
                    firstSqlLine = rawSql.split('\n').find(function (line) { return line.trim() !== ''; });
                    if (!firstSqlLine || !/^CREATE\s+(TABLE|INDEX|UNIQUE\s+INDEX)\b/i.test(firstSqlLine)) {
                        throw new Error('drizzle-kit export did not print SQL to stdout');
                    }
                    console.log("Writing ".concat(outputPath, "..."));
                    sql = [
                        '-- This file is generated by pnpm generate:sql. Do not edit manually.',
                        makeSchemaIdempotent(rawSql),
                        '',
                    ].join('\n');
                    node_fs_1.default.writeFileSync(outputPath, sql);
                    console.log('Schema SQL generated.');
                    return [2 /*return*/];
            }
        });
    });
}
main().catch(function (error) {
    console.error(error instanceof Error ? error.stack : String(error));
    process.exit(1);
});
